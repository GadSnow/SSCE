<form method="post" action="{{ path('app_course_of_life_delete', {'id': course_of_life.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course_of_life.id) }}">
	<button class="btn btn-danger mt-2">Supprimer</button>
</form>
