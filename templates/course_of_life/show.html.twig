{% extends 'base.html.twig' %}

{% block title %}CourseOfLife
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1 class="fw-bold">Informations</h1>

		<table class="table table-striped">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ course_of_life.id }}</td>
				</tr>
				<tr>
					<th>Fichier</th>
					<td>{{ course_of_life.file }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_course_of_life_index') }}" class="btn btn-secondary">Retour à la liste des CV</a>

		<a href="{{ path('app_course_of_life_edit', {'id': course_of_life.id}) }}" class="btn btn-primary">Modifier</a>

		{{ include('course_of_life/_delete_form.html.twig') }}
	</div>

{% endblock %}
