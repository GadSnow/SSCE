{% extends 'base.html.twig' %}

{% block title %}Edit Student
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="fw-bold">Modification des informations de :
			{{ student.fullname }}</h1>

		{{ include('student/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a href="{{ path('app_student_index') }}" class="btn btn-secondary">Retour à la liste des étudiants</a>

		{{ include('student/_delete_form.html.twig') }}
	</div>

{% endblock %}
