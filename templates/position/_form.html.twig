{{ form_start(form) }}
<div class="row">
	<div class="col">{{ form_row(form.label) }}</div>
	<div class="col">{{ form_row(form.situation) }}</div>
</div>
<div class="row">
	<div class="col">{{ form_row(form.studentId) }}</div>
	<div class="col">{{ form_row(form.entrepriseId) }}</div>
</div>
<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
