{% extends 'base.html.twig' %}

{% block title %}Position
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1 class="fw-bold">Informations</h1>

		<table class="table table-striped">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ position.id }}</td>
				</tr>
				<tr>
					<th>Label</th>
					<td>{{ position.label }}</td>
				</tr>
				<tr>
					<th>Situation</th>
					<td>{{ position.situation }}</td>
				</tr>
				<tr>
					<th>Date</th>
					<td>{{ position.createdAt ? position.createdAt|date('Y-m-d') : '' }}</td>
				</tr>

			</tbody>
		</table>

		<a href="{{ path('app_position_index') }}" class="btn btn-secondary">Retour à la liste des positions</a>

		<a href="{{ path('app_position_edit', {'id': position.id}) }}" class="btn btn-primary">Modifier</a>

		{{ include('position/_delete_form.html.twig') }}
	</div>

{% endblock %}
