{% extends 'base.html.twig' %}

{% block title %}Edit Speciality
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="fw-bold">Modification de la spécialité :
			{{ speciality.label}}
		</h1>

		{{ include('speciality/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a href="{{ path('app_speciality_index') }}" class="btn btn-secondary">Retour à la liste des spécialités</a>

		{{ include('speciality/_delete_form.html.twig') }}
	</div>
{% endblock %}
